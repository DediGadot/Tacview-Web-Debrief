# DCS World Mission Statistics Analyzer Requirements
# All dependencies are part of Python standard library - no external packages needed!

# The script uses only built-in Python modules:
# - re (regular expressions)
# - xml.etree.ElementTree (XML parsing)
# - json (JSON export)
# - collections (defaultdict, Counter)
# - dataclasses (dataclass decorator)
# - typing (type hints)
# - argparse (command line arguments)
# - datetime (future use)

# If you want to extend the script with additional features, you might consider:
# pandas>=1.3.0     # For advanced data analysis
# matplotlib>=3.5.0 # For plotting statistics
# plotly>=5.0.0     # For interactive charts 